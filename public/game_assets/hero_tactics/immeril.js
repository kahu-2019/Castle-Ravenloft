var heroLevel = 1

var diceRoll = 20

var isHeroPresent = true

var bonus = {
attackBonus: +1,
target: "otherHeroes"
}

if (isHeroPresent === true && diceRoll == 20 ){
    return bonus
}
